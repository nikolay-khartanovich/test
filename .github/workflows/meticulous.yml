# Файл удален, так как весь функционал перемещен в add-meticulous-button.yml

name: "Meticulous Tests"

on:
  pull_request_target:
    types: [opened, reopened]
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  actions: write
  issues: write
  statuses: write

jobs:
  # Добавляет комментарий с инструкциями при создании PR
  add-initial-comment:
    if: github.event_name == 'pull_request_target'
    runs-on: ubuntu-latest
    steps:
      - name: Add information comment
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            
            const body = `## Meticulous тесты
            
            Для запуска Meticulous тестов на этом PR, добавьте комментарий с текстом:
            
            \`\`\`
            /meticulous
            \`\`\`
            
            Другие доступные команды:
            \`/help\` или \`/помощь\` - показать справку
            `;
            
            await github.rest.issues.createComment({
              issue_number,
              owner,
              repo,
              body
            });

  # Запускает Meticulous тесты при комментарии /meticulous через workflow_dispatch
  trigger-meticulous:
    if: github.event_name == 'issue_comment' && github.event.issue.pull_request && contains(github.event.comment.body, '/meticulous')
    runs-on: ubuntu-latest
    steps:
      - name: Add rocket reaction to comment
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            
            await github.rest.reactions.createForIssueComment({
              owner,
              repo,
              comment_id: context.payload.comment.id,
              content: 'rocket'
            });

      - name: Get PR branch
        uses: xt0rted/pull-request-comment-branch@v1
        id: comment-branch

      - name: Notify PR about start
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner,
              repo,
              body: '🚀 Запускаю Meticulous тесты. Результаты будут доступны здесь после завершения.'
            });

      - name: Set latest commit status as pending
        uses: myrotvorets/set-commit-status-action@master
        with:
          sha: ${{ steps.comment-branch.outputs.head_sha }}
          token: ${{ secrets.GITHUB_TOKEN }}
          context: "Meticulous Tests"
          status: pending
          description: "Запуск Meticulous тестов..."

      - name: Get PR info
        uses: actions/github-script@v6
        id: pr-info
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            
            const { data: pr } = await github.rest.pulls.get({
              owner,
              repo,
              pull_number: issue_number
            });
            
            return {
              head_ref: pr.head.ref,
              head_sha: pr.head.sha,
              base_ref: pr.base.ref,
              base_sha: pr.base.sha,
              pr_number: issue_number
            };

      - name: Trigger Meticulous Runner Workflow
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: meticulous-runner.yml
          ref: ${{ fromJson(steps.pr-info.outputs.result).head_ref }}
          inputs: |
            {
              "head_ref": "${{ fromJson(steps.pr-info.outputs.result).head_ref }}",
              "head_sha": "${{ fromJson(steps.pr-info.outputs.result).head_sha }}",
              "base_ref": "${{ fromJson(steps.pr-info.outputs.result).base_ref }}",
              "base_sha": "${{ fromJson(steps.pr-info.outputs.result).base_sha }}",
              "pr_number": "${{ fromJson(steps.pr-info.outputs.result).pr_number }}"
            }

  # Показывает справку при комментарии /help или /помощь
  add-help-comment:
    if: github.event_name == 'issue_comment' && github.event.issue.pull_request && (contains(github.event.comment.body, '/help') || contains(github.event.comment.body, '/помощь'))
    runs-on: ubuntu-latest
    steps:
      - name: Add help comment
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const { owner, repo } = context.repo;
            
            const body = `## Команды Meticulous
            
            Доступные команды для этого PR:
            
            - \`/meticulous\` - Запустить проверку Meticulous
            - \`/help\` или \`/помощь\` - Показать эту справку
            `;
            
            await github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner,
              repo,
              body
            });
            
            await github.rest.reactions.createForIssueComment({
              owner,
              repo,
              comment_id: context.payload.comment.id,
              content: 'eyes'
            });